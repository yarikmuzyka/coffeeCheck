node_modules
.next
dist
out
.DS_Store
*.local

# env
.env
.env*.local

# prisma sqlite
prisma/*.db
prisma/*.db-journal

# local tooling / downloaded runtime (not portable — hardcoded paths, ~90MB node)
.runtime
.claude
